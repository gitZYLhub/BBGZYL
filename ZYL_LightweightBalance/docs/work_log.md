# 工作日志

## 版本 0.7.0

- 将“百花齐放”黄金时代着力点从每个特色区域 +2 文化，提升为每个特色区域 +2 文化、+1 生产力、+2 金币。
- 保留“百花齐放”鼓舞额外提供 10% 市政开销的原有效果。
- 新增两个 `MODIFIER_PLAYER_CITIES_ADJUST_CITY_YIELD_PER_DISTRICT` 修正器，分别为百花齐放提供每个特色区域 +1 生产力和 +2 金币。
- 将 `UNIT_MILITARY_ENGINEER` 的 `BaseMoves` 设置为 4，并同步更新军事工程师三语说明。
- 军事工程师修建普通道路不消耗建造次数、每格消耗 1 点铁的既有规则不变；铁路规则不变。
- 更新模组版本号至 `0.7.0`，并同步更新更新日志、修改大全、手机版、微信版和创意工坊本地文案。
- 本机没有文明 6，无法进行游戏内加载验证；本次通过 SQL 片段模拟、XML 解析、文档结构和空白检查验证。

## 版本 0.6.9

- 在风云变幻渔场调整中加入 `IMPROVEMENT_FISHERY` 对 `FEATURE_REEF` 的有效地貌许可，使礁石可建渔场。
- 保留渔场原有相邻海洋资源要求，不额外开放无资源相邻的礁石渔场。
- 将甘榜屋 `IMPROVEMENT_KAMPUNG` 的解锁科技由 `TECH_SHIPBUILDING` 改为 `TECH_CELESTIAL_NAVIGATION`。
- 加入 `IMPROVEMENT_KAMPUNG` 对 `FEATURE_REEF` 的有效地貌许可，使礁石可建甘榜屋。
- 保留甘榜屋原有相邻海洋资源要求。
- 同步更新英文、简体中文、繁体中文文本，更新版本号至 `0.6.9`，并同步更新更新日志、修改大全、手机版、微信版和创意工坊本地文案。
- 本机没有文明 6，无法进行游戏内加载验证；本次通过 SQL 片段模拟、XML 解析、文档结构和空白检查验证。

## 版本 0.6.8

- 仿照 BBG，将普通战舰 `UNIT_BATTLESHIP` 的 `Combat` 设为 65、`RangedCombat` 设为 75。
- 本次不修改战舰 `PrereqTech`，因此风云变幻规则集中仍由 `TECH_REFINING` 解锁。
- 不修改巴西特色单位“米纳斯吉拉斯”。
- 更新模组版本号至 `0.6.8`，并同步更新更新日志、修改大全、手机版、微信版和创意工坊本地文案。
- 本机仍未安装文明 6，无法进行游戏内加载实测；本次将通过 SQL 内存模拟、XML 解析和文档结构检查验证。

## 版本 0.6.7

- 港口及所有港口特色替代区域挂接 `ZYL_LBM_HARBOR_HOUSING`，提供 +2 住房。
- 灯塔及所有灯塔替代建筑的 `Housing` 设为 0，并更新普通灯塔说明。
- 造船厂让本城每个渔场 +1 生产力的既有 `ZYL_LBM_SHIPYARD_FISHERY_PRODUCTION` 规则保留，本次仅同步纳入港口线文档说明。
- 将码头 `BUILDING_SEAPORT` 的标准速度成本设为 360，并将 `PrereqTech` 改为 `TECH_STEAM_POWER`。
- 更新模组版本号至 `0.6.7`，并同步更新三语说明、更新日志、修改大全、手机版、微信版和创意工坊本地文案。
- 本机仍未安装文明 6，无法进行游戏内加载实测；本次将通过 XML 解析、SQL 内存模拟和文档结构检查验证。

## 版本 0.6.6

- 将常规总督就职时间统一规则改为全部写入 `TransitionStrength = 150`，平伽拉 / Pingala 不再保留 5 回合例外。
- 调整范围仍使用常规总督白名单：平伽拉、马格努斯、维克多、梁、瑞纳、阿玛尼、莫克夏和易卜拉欣；秘密结社特殊总督对象不参与调整。
- 同步更新平伽拉基础能力“鉴赏家 / Librarian”的英文、简体中文、繁体中文说明。
- 更新模组版本号至 `0.6.6`，并同步更新更新日志、修改大全、手机版、微信版和创意工坊本地文案。
- 本机仍未安装文明 6，无法进行游戏内加载实测；本次将通过 XML 解析、SQL 内存模拟和文档结构检查验证。

## 版本 0.6.5

### 印度尼西亚：甘榜屋

- 将 `IMPROVEMENT_KAMPUNG` 的基础 +1 生产力替换为 +1 食物，基础 +1 住房保持原版数值。
- 原版 `Kampung_FoodFishingBoats` 相邻修正器改为每座相邻渔船 +1 生产力；新增独立的 `ZYL_LBM_KAMPUNG_FISHING_BOATS_FAITH` 修正器，使同一条件额外提供 +1 信仰。
- “大规模生产”的原版额外住房未改动；将甘榜屋在“土木工程”后的 `Improvement_BonusYieldChanges` 产出由生产力改为食物，并将 `Improvement_Tourism.TourismSource` 改为 `TOURISMSOURCE_PRODUCTION`。
- 甘榜屋的造船术前置、海岸/湖泊与相邻海洋资源条件、文明专属限制和“伟大千岛之国”的区域相邻加成均未改动。
- 补齐英文、简体中文、繁体中文百科说明，更新版本号至 `0.6.5`，并同步更新更新日志、两份修改大全、微信 Word 版及创意工坊文案。

### 验证

- 静态检查应确认甘榜屋仅保留基础食物、原版相邻渔船修正器已指向生产力、新信仰修正器已挂接、土木工程奖励指向食物，且旅游来源为生产力。
- 本机未安装文明 VI，无法在游戏数据库或实际对局中执行加载验证；完整实测项已写入修改大全。

## 版本 0.6.4

### 毛利海洋出生机制回退与边缘崩溃修复（版本号不变）

- 根据文明 VI 实机日志定位地图生成失败：毛利在 `(3, 0)` 海格出生后，富饶度淡水修整于 `ZYL_RVC_Balance.lua:4252` 对地图边缘不存在的相邻格调用 `IsWater()`，导致 `GenerateMap()` 中止。
- 毛利等 `OceanStart` 文明不再加入海岸文明数量、出生带分配、队伍侧筛选或分布验证，恢复参考地图原有的 `StartPositioner.PlaceOceanStartCivs` 返回顺序直接分配海洋出生格。
- 淡水修整明确跳过所有水域出生格；`Terraforming_Water` 入口及三轮相邻格扫描均增加空值保护，避免毛利或其他边缘出生再次触发 Lua 空值错误。
- 内陆文明和陆地海岸倾向文明的分带规则保持不变；本次不改变大厅选项和模组版本号，仍为 `0.6.4`。
- 针对性静态断言确认旧海洋分带函数和报错路径已经移除、`OceanStart` 不会进入分带集合、三轮相邻水格访问均先判空；当前环境没有可用 Lua 解析器，尚未重新启动文明 VI 进行地图生成实测。
- 微信版 Word 已重建为 326 个段落，无自动编号、空标题或旧毛利规则残留；本机缺少 LibreOffice，无法执行逐页 PNG 渲染检查。

### 富饶竖向大陆毛利南北边缘限制（版本号不变）

- 本限制只写入“富饶竖向大陆（修复版）”的专用出生脚本，不改变原版地图、其他模组地图或毛利的全局规则。
- 毛利仍先由原版 `StartPositioner.PlaceOceanStartCivs` 选择海洋出生格；只有结果落入本地图南北禁区时，才改选距离原格最近的合法、可通行、非湖泊、非自然奇观远洋格，不加入文明出生带或队伍侧筛选。
- 南北禁区随地图尺寸调整：2/4/6/8 人图各 5 格，10 人图 6 格，12 人图 7 格，14/16/18 人图 8 格；东西方向不设置边缘禁区。
- 多个海洋出生文明不会重复使用同一调整后海格；若找不到合法位置，沿用地图原有重算机制。本次不改变模组版本号，仍为 `0.6.4`。
- 针对性静态断言确认限制仅识别 `LEADER_KUPE`、只检查南北坐标、四档尺寸余量和海格占用保护均已写入；尚未重新启动文明 VI 进行地图生成实测。
- 微信版 Word 已重建为 327 个段落，无自动编号、空标题或旧毛利规则残留；本机缺少 LibreOffice，无法执行逐页 PNG 渲染检查。

### 对抗模式文明出生带（版本号不变）

- 在主要文明出生系统中新增仅由 `BBS_Team_Spawn = 1/2` 启用的分布状态；随机出生和其他模式不会创建出生带、改变文明放置顺序或执行分布验证。
- 东西对抗按地图可用纵坐标划分出生带，南北对抗按可用横坐标划分；地图边缘余量继续沿用现有 5 至 8 格的尺寸规则。
- 按实际队伍分别统计内陆组和海岸组，组内出生带数量严格等于对应文明数量；陆地文明根据 `StartBiasTerrains` 中是否存在 `TERRAIN_COAST` 自动分类，兼容资料片和其他模组文明。
- 对抗模式的陆地文明改为先处理海岸组、再处理内陆组；每名文明会比较本组尚未占用的全部出生带，在最适合自身完整出生倾向的一带内选择地块，并在成功放置后独占该带。
- 毛利等 `OceanStart` 文明不计入海岸组和出生带，沿用参考地图原有的海洋出生机制。
- 前 6 次尝试严格限制在本带，第 7 次起向两侧扩展 25%，第 13 次起扩展 50%，第 19 次起扩展一整条带并允许最终队伍侧兜底；仍无合法位置时沿用原地图的整体重算和最终 Firaxis 回退。
- 陆地主要文明放置完成后，仅在东西或南北对抗中验证参与分带的玩家是否取得出生带、出生点是否位于当前允许范围以及前 18 次尝试是否位于正确队伍侧；失败即进入下一次地图尝试。
- 同时修复海洋候选不足改为陆地出生时未清空海洋文明列表、可能导致同一玩家被重复处理的问题；旁观者不会被加入陆地文明列表。
- 地图目录下 11 个 Lua 文件全部通过 `luaparse 0.3.1` 的 Lua 5.3 语法解析；9 种地图轴长与 1 至 18 条出生带组合共 162 组整数切分测试全部连续覆盖可用范围，没有空隙或重叠。
- 模式门控、陆地分类、海岸优先顺序、三阶段放宽和最终验证均通过针对性静态断言；模组版本仍为 `0.6.4`。
- 微信版 Word 已重建为 325 个段落，无自动编号，可访问性审计为 0 项问题；本机缺少 LibreOffice，无法执行逐页 PNG 渲染检查。
- 尚未启动文明 VI 对东西、南北和随机出生模式进行地图生成实测；本次不改变大厅选项和模组版本号。

### 地图出生与冰盖修正（版本号不变）

- 核对 `StartBiasTerrains` 数据来源和完整评分流程，确认 `SEAS_CIVILIZATION` 只包含具有 `TERRAIN_COAST` 出生倾向的文明，而原判断把靠海 900 万扣分错误施加给了该集合；第 12 次重算后该扣分会被跳过，因此此前实测仍可能看到海岸文明正常靠海。
- 将这项靠海扣分改为只作用于无海岸出生倾向的普通文明，使海岸文明恢复原有的 100 万至 300 万海岸评分，非海岸文明则保持强烈的内陆偏好。
- 删除对地图顶部约 22%和底部约 13%区域强行模拟冻土的坐标惩罚；真实冻土及冻土丘陵仍按文明出生倾向正常评分。
- 将东西、南北分队出生的对方区域扣分统一由 `90000000` 提高到 `120000000`；多次重算失败后仍允许放宽分区，避免无有效出生点时整张地图生成失败。
- 停止调用特征生成器的纬度冰盖流程和主地图的远洋冰岛流程；两个旧函数保留为未调用的参考实现，新生成地图不再主动放置冰盖。
- 针对性断言确认沿海反向条件和坐标冻土阈值已移除、八处分队越界判断全部使用 1.2 亿扣分、两个冰盖生成入口均无有效调用。
- 地图目录下 11 个 Lua 文件全部通过 `luaparse 0.3.1` 的 Lua 5.3 语法解析；尚未启动文明 VI 进行地图生成与多人出生实测。
- 同步更新技术版和手机版修改大全；微信版 Word 已重建为 319 个段落，无自动编号，可访问性审计为 0 项问题。
- 本机仍缺少 LibreOffice，无法执行微信版 Word 的逐页 PNG 渲染检查；本次不改变模组版本号，仍为 `0.6.4`。

### 地图道路显示名修复（版本号不变）

- 参考地图的道路等级 `1` 使用游戏内部类型 `ROUTE_MEDIEVAL_ROAD`，但该类型在文明 VI 原版中的正式显示名是“古典时代道路”。
- 修正并入地图时依据内部类型名误写出的“中世纪道路”，同步更新英文、简体中文和繁体中文大厅文本。
- 道路等级数值和地图生成逻辑没有改变；选中“古典时代道路”仍向 `RouteBuilder.SetRouteType` 传入索引 `1`。
- 同步更新技术版、手机版和微信版修改大全；本次不改变模组版本号，仍为 `0.6.4`。

### 英文名称与城邦规则补充（版本号不变）

- 将 `.modinfo` 的模组名称改为纯 ASCII 常量 `ZYL Lightweight Balance Mod`，并同步 README、英文名称行和创意工坊标题配置；中文名称继续保留在中文说明正文中。
- 九档“富饶竖向大陆（修复版）”的城邦最大值改为 `6/10/10/14/18/22/24/24/24`，默认值改为 `3/6/6/9/12/15/18/18/18`，分别映射原版决斗、极小、小型、标准、大型和巨大地图。
- 新增大厅选项“城邦放置规则”，默认值为“联机”；该选项只切换城邦候选与后处理，不修改地图现有的主要文明和团队出生算法。
- “单机”分支复用风云变幻原版 `AssignStartingPlots.__SetStartMinor` 的候选筛选与分级后备地块，并使用原版参数：城邦与主要文明距离 `6`、城邦彼此距离 `5`。
- 原版候选点选定后，继续使用地图现有的出生倾向评分在城邦之间分配候选点；地图平衡脚本不会在单机模式追加 CPL 城邦间距淘汰。
- “联机”分支保持原地图现状：城邦与主要文明约 11 格、城邦彼此至少 7 格、同类型城邦前 20 次尝试保持 21 格，并保留不合格城邦淘汰规则。
- 补充英文、简体中文、繁体中文大厅名称、说明和选项值，并同步技术版、手机版、微信版及创意工坊文案。
- 九档城邦数量映射和新大厅参数通过 XML 结构与数值断言；地图三语本地化各 58 个标签，集合一致且无重复。
- 地图目录下 11 个 Lua 文件全部通过 `luaparse 0.3.1` 的 Lua 5.3 语法解析；尚未启动文明 VI 进行地图生成实测。
- 微信版 Word 已重建为 314 个段落，新城邦说明完整且无自动编号，可访问性审计为 0 项问题；本机缺少 LibreOffice，未能执行逐页 PNG 渲染检查。
- 本次不改变模组版本号，仍为 `0.6.4`。

### 可选地图并入（版本号不变）

- 将 \`referencemods/3759491865\` 中的“Team PVP 富饶竖向大陆”修复后并入主模组，作为风云变幻规则集下的可选地图。
- 地图主脚本、BBS 衍生资源/出生点/平衡脚本和 DW 地形工具均改用 \`ZYL_RVC_\` 文件名，避免与原参考模组或其他地图的 include 文件名冲突。
- 建立地图专用尺寸域和 \`MAPSIZE_ZYL_RVC_4\`、\`MAPSIZE_ZYL_RVC_16\`、\`MAPSIZE_ZYL_RVC_18\` 类型；动作按“注册尺寸、配置参数、应用宽度、导入工具、导入主脚本”的顺序加载。
- 将富饶度限制为安全的 1-10；竞赛模式固定为 3，地脉间距修正为 6 格，并阻止竞赛模式重复叠加随机地脉。
- 修复矩形地图坐标索引、地图尺寸枚举、默认玩家数、自然奇观与大陆数量、地形/资源硬编码、最近陆地空值、河流道路遍历和半图道路扫描等问题。
- 修复大厅“极多”战略资源使用值 `2`、出生平衡脚本却检查值 `3`，导致该档保底逻辑永不触发的问题；战略资源、鱼和地脉改为通过 `GameInfo` 动态查询。
- 修复“竞赛模式”参数写入游戏配置组、主脚本却从地图配置组读取的问题，确保大厅开关能被地图生成脚本取得。
- 修复主地图、出生平衡、海岸低地和资源生成脚本中共 21 行因原文件编码损坏而未闭合的日志字符串，避免 Lua 在地图生成前直接解析失败。
- 出生点地形重塑现在会完整保护全部战略资源（包括铀）和地脉，不再使用永远不成立的资源编号区间判断。
- 不再删除全部火山，恢复大型湖泊生成；地图数据库不再覆盖宗教上限和地脉频率。
- 地图文本标签增至 58 个，并完整覆盖英文、简体中文、繁体中文。
- 地图原作者署名保留为千川白浪、号码菌、红魔族首席魔法师，工具脚本继续保留 BBS 与 DW 原始作者信息。
- 本次不改变模组版本号，仍为 \`0.6.4\`。

### 可选地图验证

- 使用游戏本机的 `DebugConfiguration.sqlite` 与 `DebugGameplay.sqlite` 进行内存模拟加载，确认 9 档尺寸、15 个大厅参数、地图尺寸数据、最多 7 个宗教、地脉频率 `12` 和宽度调整均符合预期，两套数据库外键检查均为 0 条错误。
- `.modinfo` 的动作 ID、条件引用和 37 个清单文件均完整；地图三语本地化各 58 个标签，标签集合一致且无重复。
- 11 个地图 Lua 文件通过未闭合字符串、长注释和括号配对静态扫描；本机没有可用的 Civ VI Lua 编译器，因此未执行引擎级 Lua 编译。
- 微信版 Word 已重建为 314 个段落，章节顺序与无编号要求正确，可访问性审计为 0 项问题；本机缺少 LibreOffice，未能执行逐页 PNG 渲染检查。
- 尚未启动游戏进行大厅选择、地图生成和多人出生实测，完整实测项已保留在修改大全中。

### 已完成

- 将所有地图尺寸的最大可招募大预言家数量统一为原版最高值 `7`，不再随地图尺寸变化。
- 同步更新模组管理器中的英文、简体中文、繁体中文简介，以及技术版、手机版、微信版和创意工坊文案；版本号保持 `0.6.4`。
- 新增大厅布尔选项“时代长度优化”，默认关闭，仅在迭起兴衰和风云变幻规则集中显示。
- 开启后将标准速度世界时代范围设为：远古 30-50、古典 50-60、中世纪 40-60、文艺复兴至信息时代 30-60；联机速度由游戏缩放为对应数值的一半。
- 所有玛雅城市额外 +1 住房，同时保留宫殿总计 +2 住房，使首都坐地时拥有 5 住房、分城落地时拥有 3 住房。
- 将印度尼西亚“伟大千岛之国”中海岸和湖泊对圣地的相邻加成由少量提高为标准，即每个相邻海岸或湖泊单元格提供 +1 信仰。
- 将模组版本号由 `0.6.3` 更新为 `0.6.4`，并同步三语游戏文本、README、更新日志、技术版修改大全、手机版、微信版和创意工坊本地文案。
- 本次只修改本地项目，没有创建提交或推送 GitHub。

### 实现说明

- `sql/Base/Religion.sql` 使用 `INSERT OR REPLACE ... SELECT` 为 `Maps` 表中所有已载入地图尺寸写入 `GREAT_PERSON_CLASS_PROPHET` 的 `MaxWorldInstances = 7`，包括自定义地图尺寸。
- `GreatPersonClasses.MaxPlayerInstances = 1` 未修改，因此单个文明仍不能招募第二名大预言家或创立第二个宗教。
- 大厅选项定义在 `config/EraLengthOptions.xml`，分别绑定迭起兴衰和风云变幻规则集；通过 `ConfigurationValueMatches` 控制 `sql/XP1/EraLengthOptimization.sql` 是否加载。
- 时代 SQL 只修改 `Eras_XP1.GameEraMinimumTurns` 和 `GameEraMaximumTurns`，不会改变原版倒计时长度或半数文明触发判定。
- 玛雅全城住房和宫殿住房均使用 `MODIFIER_PLAYER_CITIES_ADJUST_BUILDING_HOUSING`：全城修正器为 `Amount = 1`；宫殿原版自带 +1，模组再对拥有宫殿的城市增加 +1，使宫殿总贡献为 +2。
- 脚本会先清理 0.6.3 的旧宫殿修正器及其条件对象，再按当前数值重建，避免升级或热加载时叠加。
- 印度尼西亚四个区域使用彼此独立的相邻修正器；本次仅将 `TRAIT_NUSANTARA_COAST_HOLY_SITE` 的 `TilesRequired` 从 `2` 改为 `1`。
- 新增 `sql/DLC/Indonesia.sql`，仅在游戏启用“印度尼西亚与高棉文明及情景包”时加载。
- 原版、BBG 与 Team PVP Balanced mod 的过时代机制仅作研究，没有写入本模组。

### 验证

- 在本机最新 `DebugGameplay.sqlite` 的只读临时副本中执行宗教 SQL，确认决斗、极小、小型、标准、大型、巨型及 3 个自定义地图尺寸的 `MaxWorldInstances` 全部为 `7`，9 条 `Maps` 记录均有对应大预言家上限记录；`MaxPlayerInstances` 仍为 `1`，外键检查为 0 条错误。
- 大厅配置已通过配置数据库模拟载入：两个规则集定向参数行均使用布尔域、默认值为 `0`，配置外键检查为 0 条错误；条件动作仅在配置值为 `1` 时载入时代 SQL。
- 在本机最新 `DebugGameplay.sqlite` 的只读内存副本中载入时代 SQL，确认远古、古典、中世纪、文艺复兴、工业、现代、原子能、信息时代依次为 `30-50`、`50-60`、`40-60`、`30-60`、`30-60`、`30-60`、`30-60`、`30-60`，未来时代继续为最终时代的空值设置；时代 SQL 外键检查为 0 条错误。
- 在本机最新 `DebugGameplay.sqlite` 的只读内存副本中按模组顺序执行全部八个 SQL 文件，确认全城住房修正器为 `Amount = 1`，宫殿条件修正器为 `Amount = 1`，两者均已挂接至玛雅文明能力；宫殿原版 `Housing = 1`，因此宫殿总贡献为 2，首都与分城目标住房分别为 5 和 3。
- 确认印度尼西亚圣地的 `TilesRequired = 1`，学院、工业区和剧院广场仍为 `2`；全量 SQL 外键检查为 0 条错误。
- `.modinfo`、本地化和图标 XML 均可解析，清单中的 14 个文件无缺失；255 条本地化记录无重复，模组简介、时代选项、玛雅与印度尼西亚文本均完整覆盖英文、简体中文和繁体中文。
- 微信版 Word 已更新为版本 `0.6.4`，包含 296 个段落；章节顺序正确，无自动编号和列表样式，且包含本次宗教上限、时代选项、玛雅与印度尼西亚说明。
- 微信版 Word 可访问性审计为 0 项问题；本机未安装 LibreOffice，因此本次未能重新执行逐页 PNG 渲染检查。
- 尚未启动游戏实测大厅开关、时代面板、分城住房和区域放置预览，已在修改大全中保留对应实测项。

## 版本 0.6.3

### 已完成

- 将远古城墙、中世纪城墙和文艺复兴城墙每层外部防御生命值固定为 `50`。
- 将格鲁吉亚城堡外部防御生命值固定为 `100`，继续保持同级标准城墙的双倍数值。
- 将“钢铁”科技的都市防御外部防御生命值由 `400` 固定为 `200`。
- 军事工程师建造四类普通道路时不再消耗建造次数，每格道路精确消耗 `1` 点铁；铁路的资源成本和建造次数规则未改。
- 将模组版本号由 `0.6.2` 更新为 `0.6.3`，并同步三语游戏文本、README、更新日志、技术版修改大全、手机版、微信版和创意工坊文案。
- 宜居度每点提供 4% 加成、上限 20% 的第三项方案按要求暂不写入模组。

### 实现说明

- 三类标准城墙与“钢铁”都市防御写在基础规则脚本；格鲁吉亚城堡写在迭起兴衰内容脚本，避免缺少资料片建筑时产生依赖问题。
- 城墙使用固定值而不是将现有数值乘以 `0.5`，避免数据库重复加载时再次减半。
- 普通道路修改位于风云变幻脚本：将 `Routes_XP2.BuildWithUnitChargeCost` 设为 `0`，并清理四类道路的原资源成本后统一写入每格 `1` 点铁。
- 铁路未包含在普通道路更新语句中，原版每格 `1` 点铁和 `1` 点煤成本保持不变。
- 为三类标准城墙、格鲁吉亚城堡、“钢铁”科技和军事工程师分配模组自有说明标签，三种语言均明确展示新数值。

### 验证

- 在本机最新 `DebugGameplay.sqlite` 的只读内存副本中按模组顺序执行全部七个 SQL 文件，确认三类标准城墙均为 `50`、格鲁吉亚城堡为 `100`、“钢铁”都市防御为 `200`，四者的外部防御力仍为 `3`。
- 确认四类普通道路的建造次数成本均为 `0`，且资源成本各自只有 `1` 点铁；铁路仍为 `0` 次建造次数、`1` 点铁和 `1` 点煤。全量 SQL 外键检查为 0 条错误。
- `.modinfo`、本地化和图标 XML 均可解析，清单中的 10 个文件无缺失；246 条本地化记录无重复，本次 6 个新说明标签均完整覆盖英文、简体中文和繁体中文。
- 微信版 Word 已更新为版本 `0.6.3`，包含 281 个段落；章节顺序为城市与经济、万神殿、秘密结社、资源与改良设施、专家、文明调整、总督、兼容性与实测，无自动编号和列表样式。
- 本机缺少 LibreOffice 和 Microsoft Word，无法执行 DOCX 逐页视觉渲染；已完成生成器校验与结构检查。
- 尚未启动游戏进行界面和多人实测，已在修改大全中保留对应实测项。

## 版本 0.6.2

### 已完成

- 将地脉的 `Resources.Frequency` 从原版的 `10` 固定提高到 `12`，地图生成密度提高 20%。
- 将模组版本号由 `0.6.1` 更新为 `0.6.2`。
- 同步黄金黎明一级头衔的英文、简体中文和繁体中文说明，以及 README、更新日志、技术版修改大全、手机版、微信版和创意工坊文案。

### 实现说明

- 密度调整放在 `sql/XP2/SecretSocieties.sql` 中，只在秘密结社规则集加载时生效。
- 使用固定整数 `12`，而不是对现有值反复乘以 `1.2`，避免重复加载或数据库处理顺序造成累计增幅。
- 该参数参与地图资源生成，因此只有新生成的地图会体现密度变化，不会向已有存档补放地脉。

### 验证

- 在本机最新 `DebugGameplay.sqlite` 的只读内存副本中执行全部七个 SQL 文件，确认地脉 `Frequency` 从原版的 `10` 变为 `12`。
- 确认地脉原有的 +1 科技、+2 生产力和基础 40 金币收获记录均未受到影响，全量 SQL 外键检查为 0 条错误。
- `.modinfo`、本地化和图标 XML 均可解析，清单文件引用无缺失；228 条本地化记录无重复，黄金黎明一级头衔完整覆盖英文、简体中文和繁体中文，且三种语言均写明 20% 密度增幅。
- 微信版 Word 已更新为版本 `0.6.2`，包含 272 个段落，无自动编号和列表样式。
- 本机缺少 LibreOffice 和 Microsoft Word，无法执行 DOCX 逐页视觉渲染；已完成生成器校验与结构检查。

## 版本 0.6.1

### 已完成

- 解锁“采矿业”科技后，建造者可以在地热裂缝上修建矿山；矿山保留地热裂缝及其科技产出。
- 加入黄金黎明并解锁地脉后，建造者可以立即收获地脉，获得基础 40 金币。
- 战略资源可以收获为基础 20 生产力，奢侈资源可以收获为基础 40 金币，分别沿用原版同类生产力和金币收获的基础值。
- 奢侈资源收获权限按照种植园、营地、矿山、采石场和渔船对应的开发科技设置；战略资源直接使用各自的显示与开发科技。
- 地脉、奢侈资源和战略资源沿用原版资源坐城机制，在其上建立城市不会移除资源。
- 将模组版本号由 `0.6.0` 更新为 `0.6.1`，并同步三语文本、README、更新日志、技术版修改大全、手机版、微信版和创意工坊文案。

### 实现说明

- 地热矿山通过 `Improvement_ValidFeatures` 添加，前置科技为 `TECH_MINING`；该关系会把地热裂缝视为矿山的有效地貌，而不是清除对象。
- 地脉收获放在秘密结社条件脚本中，`PrereqTech` 留空，由黄金黎明一级头衔的地脉显示效果自然承担解锁条件。
- 奢侈与战略资源按 `ResourceClassType` 批量写入 `Resource_Harvests`；奢侈资源根据 `Improvement_ValidResources` 映射开发科技，战略资源采用 `Resources.PrereqTech`。
- `Resource_Harvests` 每种资源和产出只能设置一个科技前置；同时支持矿山和渔船的琥珀统一按“采矿业”解锁收获。
- 丁香、肉桂、化妆品、牛仔裤、香水和玩具属于非地图型特殊奢侈品，已显式排除。
- `Resource_Harvests` 只增加建造者收获操作，不修改资源放置或坐城规则，因此资源仍会保留在城市中心下方。

### 验证

- 在本机最新 `DebugGameplay.sqlite` 的只读内存副本中，按 `Core → Base Buffs → Base Pantheons → XP1 → XP2 → Maya → SecretSocieties` 顺序成功执行全部七个 SQL 文件。
- 确认地热矿山记录为 `IMPROVEMENT_MINE + FEATURE_GEOTHERMAL_FISSURE + TECH_MINING`，地脉收获记录为基础 40 金币且无额外科技前置。
- 确认生成 28 种地图奢侈资源和 7 种战略资源的收获记录；奢侈资源均为基础 40 金币，战略资源均为基础 20 生产力，六种特殊奢侈品未生成记录。
- 全量 SQL 外键检查为 0 条错误。
- `.modinfo`、本地化和图标 XML 均可解析，清单文件引用无缺失；228 条本地化记录无重复，矿山和黄金黎明一级头衔说明均完整覆盖英文、简体中文和繁体中文。
- 微信版 Word 已更新为版本 `0.6.1`，包含 271 个段落；无自动编号、无列表样式，可访问性审计为 0 项问题。
- 本机缺少 LibreOffice 和 Microsoft Word，无法执行 DOCX 逐页渲染；已完成生成器校验、结构检查与可访问性审计。

## 版本 0.6.0

### 已完成

- 新增万神殿“口述传统”，使信奉该万神殿的城市中所有种植园 +1 食物、+2 金币。
- 为“口述传统”补充独立信条对象、种植园食物与金币修正器、原版图集图标和三语本地化。
- 玛雅宫殿额外 +2 住房。
- 将“穆塔尔忠烈之女”的近首都城市收益、建城建造者和单位战斗力范围由 6 单元格扩大到 8 单元格，并将远城惩罚边界由 7 改为 9。
- 通过玛雅文明特性授予草原丘陵与平原丘陵农场许可，使其建造者无需“土木工程”即可修建；其他文明不获得该许可。
- 将模组版本号由 `0.5.1` 更新为 `0.6.0`，并同步三语文本、README、更新日志、技术版修改大全、手机版和微信版文档。

### 实现说明

- 玛雅调整放在独立的 `sql/DLC/Maya.sql` 中，仅在检测到“玛雅与大哥伦比亚包”时加载。
- 8 环边界同时更新近城 `MaxDistance` 与远城 `MinDistance`，避免距离 9 的城市落入未定义区间。
- 丘陵农场复用 `EFFECT_ADJUST_IMPROVEMENT_VALID_TERRAIN`，并只将两个修正器挂到 `TRAIT_CIVILIZATION_MAYAB`。
- 微信版生成器会自动读取手机版中的 `0.6.0` 版本号，并将新增的“文明调整”章节排在“总督”之前。

### 验证

- 在本机最新 `DebugGameplay.sqlite` 的只读内存副本中，按 `Core → Base Buffs → Base Pantheons → XP1 → XP2 → Maya → SecretSocieties` 顺序成功执行全部七个 SQL 文件。
- 确认“口述传统”生成 1 个信条、2 条信条关联、4 个修正器；种植园食物与金币增量分别为 1 和 2。
- 确认玛雅近城两个 `MaxDistance` 均为 8，远城 `MinDistance` 为 9；宫殿住房增量为 2，丘陵农场地形仅为草原丘陵和平原丘陵。
- 确认三个新增玛雅修正器仅挂接 `TRAIT_CIVILIZATION_MAYAB`；未全局修改 `Improvement_ValidTerrains`。
- 模拟迭起兴衰规则集中缺少 `MODIFIER_PLAYER_CITIES_ADJUST_IMPROVEMENT_VALID_TERRAIN` 时，玛雅脚本可自行补齐动态修正器类型并通过外键检查。
- 全量 SQL 与迭起兴衰回退模拟的外键检查均为 0 条错误。
- `.modinfo`、本地化和图标 XML 均可解析，22 条清单文件引用无缺失；222 条本地化记录无重复，本次 5 个相关文本键均完整覆盖英文、简体中文和繁体中文。
- 微信版 Word 已更新为版本 `0.6.0`，包含 256 个段落、9 个一级章节、8 行目录和 158 条具体说明；无自动编号、无列表样式，可访问性审计为 0 项问题。

## 版本 0.5.1

### 已完成

- 将平伽拉的就职时间固定为 5 回合，并将其余七位常规总督统一改为 3 回合。
- 调整范围包含马格努斯、维克多、梁、瑞纳、阿玛尼、莫克夏和易卜拉欣；秘密结社特殊总督对象保持原值。
- 删除马格努斯、瑞纳和梁此前各自设置的 4 回合覆盖，改由统一规则管理全部常规总督。
- 将炼金术会所的基础科技由 +4 提高到 +5，并删除固定 +2 生产力。
- 保留原版学院科技相邻加成镜像为金币的修正器，新增同等数值的生产力镜像修正器。
- 为八位常规总督的基础能力及炼金术会所补齐英文、简体中文和繁体中文说明。
- 将模组版本号由 `0.4.4` 更新为 `0.5.1`，并同步 README、更新日志、修改大全和创意工坊文案。

### 实现说明

- 常规总督使用显式白名单更新，避免误改秘密结社模式中同样存放于 `Governors` 表的特殊对象。
- `TransitionStrength` 使用本机原版数据库的对应值：100 为 5 回合，150 为 3 回合。
- 炼金术会所新增修正器 `ZYL_LBM_ALCHEMICAL_SOCIETY_PRODUCTION_MIRRORS_SCIENCE`，类型与原版金币镜像修正器相同，仅将目标产出改为生产力。

### 验证

- 按 `Core → Base Buffs → Base Pantheons → XP1 → XP2 → SecretSocieties` 的实际顺序，在本机最新运行时数据库的只读内存副本中成功执行全部六个 SQL 文件。
- 确认平伽拉的 `TransitionStrength` 为 100，其余七位常规总督均为 150；四个秘密结社特殊总督对象保持为 100，未被统一规则误改。
- 确认炼金术会所的 `Building_YieldChanges` 只剩 `YIELD_SCIENCE = 5`，不存在固定生产力产出行。
- 确认炼金术会所同时挂接原版金币镜像和本模组生产力镜像，两者均以 `YIELD_SCIENCE` 相邻加成为镜像来源。
- 全量 SQL 执行后的外键检查为 0 条错误。
- `.modinfo` 与本地化 XML 均通过解析；当前 69 个文本键、207 条唯一记录无重复，本次涉及的八位常规总督和炼金术会所均完整覆盖英文、简体中文和繁体中文。
- 确认 `.modinfo`、README、更新日志、修改大全、工作日志和创意工坊文案的当前版本均为 `0.5.1`，`git diff --check` 无空白错误。

### 手机版文档补充（版本号不变）

- 新增 `docs/full_changes_mobile.md`，将当前全部玩家可见规则改写为适合手机阅读的短段落和浅层列表。
- 新增 `docs/ZYL轻量化平衡模组_修改大全_微信版_无编号版.docx`，用于直接发送到微信并通过内置文档预览阅读。
- Word 版本采用接近手机屏幕比例的窄页、12.5 磅微软雅黑正文、短段落和页码，不使用需要横向缩放的表格。
- 手机版本保留快速目录，并省略版本速览、数据库标识、提交号等技术细节；技术完整版 `full_changes.md` 继续保留。
- 微信版 Word 的章节顺序调整为“城市与经济、万神殿、秘密结社、资源与改良设施、专家、总督、兼容性与实测”，并删除“版本速览”以及没有实际改动的“文明调整”章节。
- 最终微信版去掉全部数字编号和黑点标记，仅使用章节标题、子标题、缩进与留白表达层级。
- 删除“不增加专家槽位”“不增加专家伟人点”以及其他“效果不变、保持原版、不受影响”等纯未修改说明；手机版和微信版只列出相对原版的实际改动。
- 将上述规则写入 Word 生成脚本；脚本发现纯未修改表述、遗漏章节或未纳入排序的新章节时会报错，并自动读取手机版源文档中的版本号。
- 此后每次模组规则更新都必须同步更新 `full_changes.md`、`full_changes_mobile.md`，并重新生成微信版 Word 文档。
- Word 文件通过 ZIP/OOXML 完整性、页面尺寸、样式、页码、章节顺序、无编号结构和可访问性检查；243 个段落均无编号属性，7 行目录和 149 条具体说明仅通过样式表达层级，可访问性审计为 0 项问题。
- 本机没有 LibreOffice、Microsoft Word 或 WPS 的可调用渲染组件，因此未能生成逐页 PNG 进行视觉复核。
- README 与技术完整版均补充手机版入口；本次仅调整文档，不改变模组规则和版本号。

## 版本 0.4.4

### 新增万神殿加载与选择修复（版本号不变）

- 本机现存最新一局日志的运行时数据库中没有任何 `BELIEF_ZYL_LBM_*` 记录，且载入组件清单未出现本模组；确认本地工程或创意工坊副本必须先在“额外内容”中启用，并且不应同时保留旧副本。
- 发现四个新增万神殿的图标错误引用了不存在的 `ICON_ATLAS_BELIEFS`；选择器先写入第一项“月亮女神”的标签、再查找图标、最后才注册按钮回调，因此图标错误恰好会造成“只显示月亮女神、无法点击、后续三项不显示”的完整现象。
- 参照原版与 Religion Expanded，图标改用原版实际存在的 `ICON_ATLAS_BELIEFS_PATHEON`，并补充图标加载顺序。
- 将新增万神殿从 `sql/Base/Buffs.sql` 拆分到 `sql/Base/Pantheons.sql`，保留原有数值，显式写入完整的信条和修正器链路。
- 为 Team PVP Tools（Workshop `3693899014`）增加条件兼容层：只在该模组启用时覆盖万神殿选择器入口，保留原界面并改为在其他玩家创建万神殿后安全重建列表。
- 修正器仍沿用 Religion Expanded 已验证的“信条挂接全城市/全玩家修正器，再附加实际效果修正器”结构。
- 英文、简体中文和繁体中文同步补充三项地块效果的生效范围说明；万神殿名称和数值均不改变。
- 同步更新 README、更新日志和修改大全；模组版本号保持 `0.4.4`。
- 按 `Core → Base Buffs → Base Pantheons → XP1 → XP2 → SecretSocieties` 的顺序在本机最新运行时数据库的只读内存副本中加载全部 SQL，六个脚本均成功执行。
- 确认生成 4 个新增万神殿、5 条信条关联、10 个修正器和 14 条修正器参数；所有修正器类型与条件集均存在，太阳神正确覆盖小麦、大米和玉米，外键检查为 0 条错误。
- 确认 `.modinfo`、图标 XML 和本地化 XML 均可解析，清单文件引用无缺失；186 条本地化记录无重复，四个新增万神殿的名称与说明均完整覆盖英文、简体中文和繁体中文。
- Team PVP Tools 兼容层已完成结构与载入条件检查；仍需在启用本模组和 Team PVP Tools 的新游戏中进行最终界面点击实测。

### 秘密结社发现概率补充（版本号不变）

- 根据本机原版 `SecretSocieties` 表确认四类发现字段的准确名称，完成版本 `0.4` 暂缓的结社发现概率调整。
- 弥涅耳瓦夜鹰仅将 `DiscoverAtCityStateBaseChance` 从 80 提高到 `100000`。
- 黄金黎明/赫尔墨斯教团仅将 `DiscoverAtNaturalWonderBaseChance` 从 100 提高到 `100000`。
- 虚空吟唱者仅将 `DiscoverAtGoodyHutBaseChance` 从 70 提高到 `100000`。
- 血色契约仅将 `DiscoverAtBarbarianCampBaseChance` 从 70 提高到 `100000`。
- 每个结社的其他发现来源字段保持为 0，原版发现条件与外交能见度间接发现机制均不改变。
- 为四种原版发现来源补充英文、简体中文和繁体中文文明百科说明。
- 同步更新修改大全和更新日志；模组版本号保持 `0.4.4`。

### 已完成

- 将马格努斯就职时间由 5 回合缩短为 4 回合。
- “盈余物流”保留人口增长与贸易路线 +2 食物，新增贸易路线 +1 生产力，并并入开拓者不消耗人口的效果。
- “给养保障”移除原开拓者效果，改为此城建造单位、区域和非奇观建筑时 +20% 生产力。
- “实业家”保留名称、位置与前置关系，效果替换为工作坊 +2 生产力、工厂 +4 生产力，以及风云变幻规则集中的发电厂 +7 生产力和每项资源 +4 电力。
- 保持“拓荒者”“黑市商人”“垂直整合”及马格努斯晋升树的其他内容不变。
- 为三项修改后的晋升同步补充英文、简体中文和繁体中文说明。
- 将模组版本号更新为 `0.4.4`，并同步 README、更新日志和修改大全。

### 实现说明

- 新增修正器统一使用 `ZYL_LBM_` 命名空间，避免与 BBG 或其他模组的数据库主键冲突。
- “给养保障”的建筑生产加成沿用 BBG 同类效果的 `IsWonder = 0` 限制，因此不影响奇观。
- “实业家”的工作坊、工厂和发电厂数值参考 Better Balanced Game 提交 `7b3cddbe`；发电厂修正器仅在规则集提供相应对象时挂接，以兼容迭起兴衰。

### 验证

- 按 `Core → Base → XP1 → XP2 → SecretSocieties` 的实际顺序将全部 SQL 应用于本机原版游戏数据库的内存副本，所有脚本成功加载且外键检查为 0 条错误。
- 确认黄金黎明、弥涅耳瓦夜鹰、血色契约和虚空吟唱者分别只有自然奇观、城邦、蛮族营地和部落村庄来源为 `100000`，其余三类来源均保持为 0。
- 确认四条秘密结社发现百科说明均完整覆盖英文、简体中文和繁体中文，当前本地化总计 62 个文本键、186 条唯一记录。
- 确认马格努斯就职强度为 125（4 回合），“盈余物流”“给养保障”“实业家”分别只挂接 4、3、6 个预期修正器。
- 确认贸易路线食物为 +2、生产力为 +1，三类生产项目均为 +20%，且建筑修正器包含 `IsWonder = 0`。
- 确认工作坊、工厂和三类发电厂的生产力分别为 +2、+4、+7，发电厂每项资源提供的电力为 +4。
- 确认马格努斯全部晋升的层级、位置和前置关系未改变，“拓荒者”“黑市商人”“垂直整合”的修正器关联未改变。
- `.modinfo` 和本地化 XML 均通过解析；当前 62 个文本键、186 条唯一记录均完整覆盖英文、简体中文和繁体中文，自定义文本键无缺失。
- 确认 `.modinfo`、README、更新日志、修改大全和工作日志的当前版本均为 `0.4.4`，`git diff --check` 无空白错误。

## 版本 0.4.3

### 已完成

- 将 Better Balanced Game 提交 `7b3cddbe` 中的瑞纳重做完整移植到 `sql/XP1/Buffs_XP1.sql`，同时适用于迭起兴衰和风云变幻规则集。
- 将瑞纳就职时间改为 4 回合，并移植土地征用、港务局长、林业管理和税务官的数值与附加效果。
- 为“承包商”增加建筑金币购买五折，并按数据库中的 `FORMATION_CLASS_SUPPORT` 动态生成全部支援单位金币购买五折修正器。
- 删除“可再生能源”晋升及其前置、晋升树和修正器关联，以新的“外汇交易”晋升替代。
- “外汇交易”基础提供每条经过城市的外国贸易路线 +1 科技、+1 文化，研究经济学后通过第二组修正器提高到各 +2。
- 为瑞纳六项能力补充英文与简体中文说明。
- 将模组版本号更新为 `0.4.3`，并同步 README、更新日志和修改大全。

### 文本与本地化修复（版本号不变）

- 修正“百花齐放”的英文、简体中文和繁体中文说明，使其准确显示每个特色区域 +2 文化。
- 为四个秘密结社的 2、3、4 级头衔补充英文、简体中文和繁体中文说明，写明 3 级在文艺复兴时代解锁、4 级在工业时代解锁，以及选择后返还 1 个总督点。
- 修正黄金黎明/赫尔墨斯教团 3 级头衔的地脉伟人收益说明：海军统帅提供食物、大将军提供生产力、大商人提供 +2 金币。
- 为此前全部英文和简体中文文本补充 `zh_Hant_HK` 繁体中文版本；当前 55 个本地化键均同时覆盖英文、简体中文和繁体中文。
- 将 `.modinfo` 中的名称、简介和短说明改为引用 `LOC_ZYL_LBM_NAME`、`LOC_ZYL_LBM_DESCRIPTION` 和 `LOC_ZYL_LBM_TEASER`，修复中文环境下模组简介仍显示英文的问题。
- 将前端和游戏内文本加载顺序提高到 `4000000`，避免 Team PVP Balanced mod 的晚加载文本覆盖本模组实际规则说明。
- 本次仅修复文本和本地化，不修改数据库规则；模组版本号保持 `0.4.3`，不更新 README、更新日志或修改大全。

### 实现说明

- 新晋升、条件集和修正器统一使用 `ZYL_LBM_` 命名空间，数值与作用范围保持 BBG 一致。
- BBG 西班牙语说明将第二阶段科技写成“银行业”，但 SQL 注释和所有者条件实际使用经济学；本模组按实际数据库行为使用 `TECH_ECONOMICS` 并显示“经济学”。
- 林业管理使用任一条件集，因此单元格同时拥有未改良地貌和惊艳魅力时仍只获得一次 +2 金币。

### 验证

- 按 `Core → Base → XP1 → XP2` 的实际顺序执行完整数据库导入，所有脚本成功加载且外键检查为 0 条错误。
- 确认瑞纳就职强度为 125（4 回合），原版晋升树结构保留，右侧最终晋升已由“可再生能源”替换为“外汇交易”。
- 确认土地征用金币为 +4，林业管理使用“未改良地貌或惊艳魅力”任一条件集，港务局长和税务官分别挂接 +1 贸易路线容量。
- 确认承包商为当前数据库全部 10 类支援单位生成金币购买五折修正器，并挂接建筑金币购买五折修正器。
- 确认外汇交易挂接 4 个收益修正器，第二阶段条件准确绑定 `TECH_ECONOMICS`。
- `.modinfo` 和本地化 XML 均通过解析，本地化键无重复，`git diff --check` 无空白错误。
- 本地化文件现有 55 个文本键、165 条唯一记录，英文、简体中文和繁体中文均完整覆盖；SQL 与 `.modinfo` 引用的自定义文本键全部存在。
- 确认模组元数据已引用本地化键，前端和游戏内文本加载顺序均为 `4000000`，模组版本仍为 `0.4.3`。
- 自动检查“百花齐放”三语说明均包含 +2 文化，并确认四个秘密结社的三级、四级时代、头衔返还和黄金黎明地脉收益说明与数据库规则一致。

## 版本 0.4.2

### 已完成

- 将 Better Balanced Game 提交 `7b3cddbe` 中的梁重做完整移植到 `sql/XP2/Buffs_XP2.sql`，仅在风云变幻规则集中加载。
- 将梁就职时间改为 4 回合，重排“加固材料”“公园与休闲”“土地规划委员”和最终晋升的层级与前置。
- 删除“水产养殖”，新增“农业”晋升；资源右侧分支合计为每个已发现资源 +1 食物、+1 生产力。
- 重做城市公园的科技、文化、住房、宜居度、地形和每城数量限制。
- 将渔场移动到天文导航科技，取消梁的建造限制，提高住房，并加入造船厂 +1 生产力效果。
- 为新晋升、城市公园和渔场补充英文与简体中文说明。
- 将模组版本号更新为 `0.4.2`，并同步 README、更新日志和修改大全。

### 实现说明

- BBG 的梁 SQL 依赖其他文明文件建立的泛滥平原条件；本模组在 XP2 文件中独立建立了包含沙漠、草原和平原泛滥平原的命名空间条件集。
- 新增对象统一使用 `ZYL_LBM_` 命名空间，数值和作用范围与 BBG 保持一致。
- 删除“水产养殖”时显式清理所有以其为前置的记录，避免在外键级联未启用的 SQLite 环境中留下无效前置。

### 验证

- `sql/XP2/Buffs_XP2.sql` 已成功应用于开启外键约束的原版游戏数据库内存副本，外键检查为 0 条错误。
- 确认梁的最终晋升树为基础能力、两个一级分支、两个二级分支和中央三级最终晋升，“水产养殖”已完全删除。
- 确认城市公园只允许五种平地、每城限建一个，并具有 +2 文化、+3 科技、无条件 +1 宜居度及新增 +2 住房修正器。
- 确认渔场由天文导航解锁、无文明特质限制、住房数据库值为 2（游戏显示 +1），并保留 +1 食物和海洋资源相邻食物。
- 确认渔场不再带有梁的改良修正器，造船厂正确挂接渔场 +1 生产力修正器。
- 按 `Core → Base → XP1 → XP2` 的实际顺序执行完整数据库导入，所有脚本成功加载且外键检查为 0 条错误。

## 版本 0.4.1

### 已完成

- 将 Better Balanced Game 提交 `7b3cddbe` 中的维克多修改完整移植到 `sql/XP1/Buffs_XP1.sql`，同时适用于迭起兴衰和风云变幻规则集。
- 移植维克多的忠诚度压力、驻军司令数值、防御后勤替换、射击孔与军备竞赛倡导者分支重排，以及防空计划最终前置。
- 将 BBG 新增的军事单位生产修正器改用 `ZYL_LBM_VICTOR_UNIT_PRODUCTION` 标识，避免与 BBG 同时加载时发生主键冲突，数值和作用范围保持一致。
- 为维克多六项能力补充英文和简体中文游戏内说明。
- 将模组版本号更新为 `0.4.1`，并同步 README、更新日志和修改大全。

### BBG 源码核对

- BBG 的维克多 SQL 注释和西班牙语文本将核武器项目加成写为 +50%，但固定提交中没有修改原版四个核项目修正器的 `Amount`。
- 本机原版游戏数据库中对应数值为 30，因此本模组照搬 BBG 的实际数据库行为并在游戏内准确显示为 +30%。

### 验证

- `.modinfo` 和本地化 XML 均通过 XML 解析。
- 将 `sql/XP1/Buffs_XP1.sql` 应用于本机原版游戏数据库的内存副本后，确认维克多的数值、晋升层级、前置关系和修正器均符合预期。
- 确认“射击孔”只保留额外城市攻击并获得防围攻；“军备竞赛倡导者”获得免费晋升和 100% 战略资源折扣；“防空计划”同时要求两条二级分支。

## 版本 0.4

### 已完成

- 参考 Team PVP Balanced mod 结社 3.93 的写法，完成秘密结社 3 级头衔提前到文艺复兴时代。
- 将秘密结社 4 级头衔提前到工业时代。
- 保留并整理 2、3、4 级结社头衔免费化逻辑。
- 镀金宝库取消科技前置和金币购买前置字段，保留夜鹰 2 级头衔解锁。
- 炼金协会取消科技前置和金币购买前置字段，保留黄金黎明/赫尔墨斯教团 2 级头衔解锁。
- 地脉本身新增 +1 科技、+2 生产力。
- 确认地脉原版出现频率已经为 10，不再重复覆盖；有效地形删除列表照搬 Team PVP Balanced mod 结社 3.93。
- 参考 Team PVP Balanced mod 结社 3.93，补充书院 `DISTRICT_SEOWON` 对 `LeyLine_Science` 的地脉科技相邻。
- 伟人给地脉产出时，海军统帅改给食物，大将军改给生产力，大商人金币数值改为 +2。
- 修正规则集动作条件的 `.modinfo` 写法，并为秘密结社动作增加模式开启条件。
- 秘密结社修改同时支持迭起兴衰和风云变幻规则集。
- 为证券交易所每人口金币效果注册本模组自己的基础规则 Modifier 类型。
- 为地脉资源和书院相邻插入增加对象存在性检查。

### 暂缓

- 结社发现条件不改、但发现概率大幅提升。
- 后续状态：该项已在版本 `0.4.4` 的版本号不变补充中完成；本节保留为版本 `0.4` 当时的历史记录。

### 暂缓原因

- 本机没有文明 6 原版数据库，无法直接确认 `SecretSocieties` 表中各发现来源的准确字段名。
- 参考 mod 中，Team PVP Balanced mod 结社 3.93 只明确使用了 `DiscoverAtCityStateBaseChance`。
- TeamPVP 的实际做法是把四个结社都写到城邦发现概率上，这适合它的联机设计，但会改变本模组想要保留的原版发现条件。
- 为避免猜测 `DiscoverAtNaturalWonderBaseChance`、`DiscoverAtTribalVillageBaseChance`、`DiscoverAtBarbarianCampBaseChance` 等字段导致数据库报错，本版不写入任何发现概率 SQL。

### 后续实现条件

- 拿到文明 6 原版秘密结社模式数据库文件。
- 或者拿到一次启用本模组后的 `Database.log` / `Modding.log`，用日志确认字段名。
- 确认字段后，再分别提高：
  - 弥涅耳瓦夜鹰的原版发现来源概率。
  - 黄金黎明/赫尔墨斯教团的原版发现来源概率。
  - 虚空吟唱者的原版发现来源概率。
  - 血色契约的原版发现来源概率。
